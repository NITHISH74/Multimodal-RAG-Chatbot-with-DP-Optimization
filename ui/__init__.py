"""Streamlit UI components, split out of app.py.

Modules
-------
components       shared CSS + status chips, metric tiles, source cards
sidebar          upload, crawl, history, identity
master_settings  the Master Settings panel (model switch, multimodal mode, etc.)
chat             chat surface, message rendering, feedback controls
"""
